name: branch-proof-hardening-build

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  manual-placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Branch proof runs through the temporary config-surface workflow."
